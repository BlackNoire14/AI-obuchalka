# Инструкция по деплою CodeTutor

Это руководство поможет развернуть полный стек приложения CodeTutor в облаке.

## Архитектура деплоя

- **Frontend**: Netlify (статический хостинг React)
- **Backend**: Render.com (Node.js API)
- **Database**: MongoDB Atlas (облачная база данных)

---

## Шаг 1: Настройка MongoDB Atlas (База данных)

### 1.1 Создание аккаунта и кластера

1. Перейдите на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas/register)
2. Зарегистрируйтесь (бесплатно)
3. Создайте новый проект: **CodeTutor**
4. Нажмите **"Build a Database"**
5. Выберите **FREE tier (M0)**
6. Выберите регион (например, AWS / Frankfurt)
7. Назовите кластер: **code-tutor-cluster**
8. Нажмите **"Create"**

### 1.2 Настройка доступа

1. **Database Access** (пользователь БД):
   - Перейдите в **Database Access** → **Add New Database User**
   - Username: `codetutor`
   - Password: сгенерируйте сложный пароль (сохраните его!)
   - Database User Privileges: **Read and write to any database**
   - Нажмите **Add User**

2. **Network Access** (разрешить подключения):
   - Перейдите в **Network Access** → **Add IP Address**
   - Нажмите **"Allow Access from Anywhere"** (0.0.0.0/0)
   - Нажмите **Confirm**

### 1.3 Получение строки подключения

1. Перейдите в **Database** → **Connect**
2. Выберите **"Connect your application"**
3. Driver: **Node.js**, Version: **4.1 or later**
4. Скопируйте строку подключения:
   ```
   mongodb+srv://codetutor:<password>@code-tutor-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. Замените `<password>` на ваш пароль
6. Добавьте имя базы данных перед `?`:
   ```
   mongodb+srv://codetutor:ВАШ_ПАРОЛЬ@code-tutor-cluster.xxxxx.mongodb.net/code-tutor?retryWrites=true&w=majority
   ```

✅ **Сохраните эту строку - она понадобится для backend!**

---

## Шаг 2: Деплой Backend на Render.com

### 2.1 Подготовка репозитория

1. Создайте Git репозиторий (если еще не создан):
   ```bash
   cd code-tutor
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. Создайте репозиторий на GitHub:
   - Перейдите на [GitHub](https://github.com/new)
   - Назовите репозиторий: **code-tutor**
   - Нажмите **Create repository**

3. Загрузите код на GitHub:
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/code-tutor.git
   git branch -M main
   git push -u origin main
   ```

### 2.2 Создание Web Service на Render

1. Перейдите на [Render.com](https://render.com/) и зарегистрируйтесь
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий **code-tutor**
4. Настройте параметры:
   - **Name**: `code-tutor-backend`
   - **Region**: Frankfurt (EU Central)
   - **Branch**: `main`
   - **Root Directory**: `backend`
   - **Runtime**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Instance Type**: `Free`

### 2.3 Настройка переменных окружения

В разделе **Environment Variables** добавьте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `5000` |
| `MONGODB_URI` | Ваша строка подключения из MongoDB Atlas |
| `JWT_SECRET` | Сгенерируйте случайную строку (минимум 32 символа) |
| `JWT_EXPIRES_IN` | `30d` |
| `FRONTEND_URL` | `https://ваш-сайт.netlify.app` (обновите после деплоя frontend) |
| `OPENAI_API_KEY` | Ваш OpenAI API ключ (опционально) |
| `OPENAI_MODEL` | `gpt-4o-mini` |

**Для генерации JWT_SECRET** используйте:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 2.4 Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (5-10 минут)
3. После успешного деплоя вы получите URL:
   ```
   https://code-tutor-backend.onrender.com
   ```

✅ **Сохраните этот URL - он понадобится для frontend!**

### 2.5 Проверка

Откройте в браузере:
```
https://code-tutor-backend.onrender.com/
```

Вы должны увидеть:
```json
{"message": "Добро пожаловать в API Code Tutor"}
```

---

## Шаг 3: Деплой Frontend на Netlify

### 3.1 Обновление конфигурации frontend

1. Обновите файл `frontend/.env`:
   ```env
   REACT_APP_API_BASE=https://code-tutor-backend.onrender.com
   ```

2. Закоммитьте изменения:
   ```bash
   git add frontend/.env
   git commit -m "Update API URL for production"
   git push
   ```

### 3.2 Деплой на Netlify

1. Перейдите на [Netlify](https://www.netlify.com/) и зарегистрируйтесь
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и подключите репозиторий **code-tutor**
4. Настройте параметры:
   - **Base directory**: `frontend`
   - **Build command**: `npm run build`
   - **Publish directory**: `frontend/build`
   - **Environment variables**:
     - `REACT_APP_API_BASE` = `https://code-tutor-backend.onrender.com`

5. Нажмите **"Deploy site"**

### 3.3 Настройка домена (опционально)

1. После деплоя вы получите URL вида:
   ```
   https://random-name-12345.netlify.app
   ```

2. Можете изменить имя:
   - **Site settings** → **Change site name**
   - Например: `code-tutor-app`
   - Новый URL: `https://code-tutor-app.netlify.app`

### 3.4 Обновление CORS в Backend

1. Вернитесь в Render.com → ваш backend service
2. Обновите переменную `FRONTEND_URL`:
   ```
   https://code-tutor-app.netlify.app
   ```
3. Сохраните (сервис автоматически перезапустится)

---

## Шаг 4: Финальная проверка

### 4.1 Проверка работы приложения

1. Откройте ваш frontend: `https://code-tutor-app.netlify.app`
2. Попробуйте зарегистрироваться
3. Войдите в систему
4. Проверьте уроки и практику
5. Попробуйте решить задачу и сохранить прогресс

### 4.2 Проверка API

Проверьте endpoints:
```bash
# Проверка главной страницы
curl https://code-tutor-backend.onrender.com/

# Проверка регистрации
curl -X POST https://code-tutor-backend.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123","name":"Test User"}'
```

---

## Важные замечания

### 🔄 Автоматический деплой

- **Render**: автоматически деплоит при push в `main` ветку
- **Netlify**: автоматически деплоит при push в `main` ветку

### 💰 Бесплатные лимиты

- **MongoDB Atlas**: 512 MB хранилища (достаточно для начала)
- **Render**: 750 часов в месяц (сервис засыпает после 15 минут неактивности)
- **Netlify**: 100 GB трафика в месяц

### ⚡ "Холодный старт" на Render

На бесплатном плане Render сервис засыпает после 15 минут неактивности. Первый запрос после сна займет 30-60 секунд.

**Решение**: используйте сервис мониторинга (например, UptimeRobot) для пинга каждые 10 минут.

### 🔒 Безопасность

1. **Никогда не коммитьте `.env` файлы** в Git
2. **Используйте сильные пароли** для MongoDB и JWT_SECRET
3. **Регулярно обновляйте зависимости**: `npm audit fix`

---

## Troubleshooting

### Backend не запускается

1. Проверьте логи в Render Dashboard
2. Убедитесь, что `MONGODB_URI` правильный
3. Проверьте, что все переменные окружения установлены

### Frontend не подключается к Backend

1. Проверьте `REACT_APP_API_BASE` в Netlify
2. Убедитесь, что `FRONTEND_URL` в Render содержит правильный URL
3. Проверьте CORS настройки

### База данных не подключается

1. Проверьте Network Access в MongoDB Atlas (должен быть 0.0.0.0/0)
2. Проверьте правильность пароля в строке подключения
3. Убедитесь, что пользователь БД создан

---

## Полезные ссылки

- [MongoDB Atlas Docs](https://docs.atlas.mongodb.com/)
- [Render Docs](https://render.com/docs)
- [Netlify Docs](https://docs.netlify.com/)

---

## Готово! 🎉

Ваше приложение CodeTutor теперь доступно онлайн!

- **Frontend**: https://code-tutor-app.netlify.app
- **Backend**: https://code-tutor-backend.onrender.com
- **Database**: MongoDB Atlas

Поделитесь ссылкой с друзьями и начните обучение программированию!
