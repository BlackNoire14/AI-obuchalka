# 🚀 Полноценный деплой CodeTutor (Netlify + Render + MongoDB Atlas)

## Время: ~20 минут | Стоимость: Бесплатно

---

## 📋 Что вам понадобится

1. Аккаунт GitHub (уже есть: BlackNoire14)
2. Аккаунт Netlify (создадим)
3. Аккаунт Render (создадим)
4. Аккаунт MongoDB Atlas (создадим)

---

## Часть 1: MongoDB Atlas (База данных) - 5 минут

### Шаг 1: Создайте аккаунт

1. Перейдите на https://www.mongodb.com/cloud/atlas/register
2. Зарегистрируйтесь (можно через Google/GitHub)
3. Выберите **FREE** план (M0 Sandbox)

### Шаг 2: Создайте кластер

1. После регистрации нажмите **"Build a Database"**
2. Выберите **FREE** (M0)
3. Provider: **AWS**
4. Region: выберите ближайший (например, Frankfurt)
5. Cluster Name: `code-tutor`
6. Нажмите **"Create"**

### Шаг 3: Настройте доступ

1. **Database Access** (слева в меню):
   - Add New Database User
   - Username: `codetutor`
   - Password: **Сгенерируйте и СОХРАНИТЕ!** (например: `Abc123456789`)
   - Database User Privileges: **Read and write to any database**
   - Add User

2. **Network Access** (слева в меню):
   - Add IP Address
   - Выберите **"Allow Access from Anywhere"** (0.0.0.0/0)
   - Confirm

### Шаг 4: Получите Connection String

1. Вернитесь в **Database** → **Connect**
2. Выберите **"Connect your application"**
3. Driver: **Node.js**, Version: **4.1 or later**
4. Скопируйте строку подключения:
   ```
   mongodb+srv://codetutor:<password>@code-tutor.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. **Замените `<password>` на ваш реальный пароль!**
6. **СОХРАНИТЕ эту строку** - она понадобится для Render

**Пример готовой строки:**
```
mongodb+srv://codetutor:Abc123456789@code-tutor.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

✅ **MongoDB Atlas готов!**

---

## Часть 2: Render (Backend) - 7 минут

### Шаг 1: Создайте аккаунт

1. Перейдите на https://render.com/
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь через **GitHub** (рекомендуется)
4. Авторизуйте Render для доступа к репозиториям

### Шаг 2: Создайте Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш репозиторий: `BlackNoire14/code-tutor`
3. Если не видите репозиторий:
   - Нажмите **"Configure account"**
   - Дайте доступ к репозиторию

### Шаг 3: Настройте сервис

Заполните форму:

- **Name**: `code-tutor-backend`
- **Region**: Frankfurt (EU Central)
- **Branch**: `main`
- **Root Directory**: `backend`
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Instance Type**: **Free**

### Шаг 4: Добавьте переменные окружения

Нажмите **"Advanced"** → **"Add Environment Variable"**

Добавьте следующие переменные:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `MONGODB_URI` | `mongodb+srv://codetutor:ВАШ_ПАРОЛЬ@code-tutor.xxxxx.mongodb.net/?retryWrites=true&w=majority` |
| `JWT_SECRET` | `super_secret_jwt_key_change_in_production_12345` |
| `JWT_EXPIRES_IN` | `30d` |
| `PORT` | `10000` |
| `FRONTEND_URL` | `https://code-tutor-blacknoire14.netlify.app` (обновим позже) |

**⚠️ ВАЖНО:** 
- Замените `ВАШ_ПАРОЛЬ` в `MONGODB_URI` на реальный пароль от MongoDB!
- `FRONTEND_URL` обновим после деплоя фронтенда

### Шаг 5: Создайте сервис

1. Нажмите **"Create Web Service"**
2. Подождите 3-5 минут (идет деплой)
3. После успешного деплоя скопируйте URL:
   ```
   https://code-tutor-backend.onrender.com
   ```
4. **СОХРАНИТЕ этот URL** - он понадобится для Netlify

✅ **Backend на Render готов!**

---

## Часть 3: Netlify (Frontend) - 5 минут

### Шаг 1: Создайте аккаунт

1. Перейдите на https://www.netlify.com/
2. Нажмите **"Sign up"**
3. Зарегистрируйтесь через **GitHub**

### Шаг 2: Создайте новый сайт

1. В Dashboard нажмите **"Add new site"** → **"Import an existing project"**
2. Выберите **"Deploy with GitHub"**
3. Авторизуйте Netlify
4. Выберите репозиторий: `BlackNoire14/code-tutor`

### Шаг 3: Настройте деплой

Заполните форму:

- **Branch to deploy**: `main`
- **Base directory**: `frontend`
- **Build command**: `npm run build`
- **Publish directory**: `frontend/build`

### Шаг 4: Добавьте переменные окружения

Нажмите **"Show advanced"** → **"New variable"**

Добавьте:

| Key | Value |
|-----|-------|
| `REACT_APP_API_BASE` | `https://code-tutor-backend.onrender.com` |

**⚠️ ВАЖНО:** Используйте URL вашего backend с Render!

### Шаг 5: Деплой

1. Нажмите **"Deploy site"**
2. Подождите 2-3 минуты
3. После деплоя скопируйте URL сайта (например):
   ```
   https://spontaneous-unicorn-abc123.netlify.app
   ```

### Шаг 6: Настройте красивый URL (опционально)

1. В настройках сайта → **"Domain settings"**
2. **"Options"** → **"Edit site name"**
3. Измените на: `code-tutor-blacknoire14`
4. Теперь URL: `https://code-tutor-blacknoire14.netlify.app`

✅ **Frontend на Netlify готов!**

---

## Часть 4: Финальная настройка - 3 минуты

### Обновите FRONTEND_URL в Render

1. Вернитесь в Render Dashboard
2. Откройте ваш сервис `code-tutor-backend`
3. Перейдите в **"Environment"**
4. Найдите `FRONTEND_URL` и обновите на:
   ```
   https://code-tutor-blacknoire14.netlify.app
   ```
5. Нажмите **"Save Changes"**
6. Сервис автоматически перезапустится

### Проверьте работу

1. Откройте ваш сайт: `https://code-tutor-blacknoire14.netlify.app`
2. Попробуйте зарегистрироваться
3. Решите задачу
4. Проверьте, что прогресс сохраняется

---

## ✅ Готово! Ваше приложение работает!

### 🎯 Ваши URL:

- **Фронтенд**: https://code-tutor-blacknoire14.netlify.app
- **Бэкенд**: https://code-tutor-backend.onrender.com
- **База данных**: MongoDB Atlas (облако)

### 🎉 Что теперь работает:

✅ Регистрация и авторизация  
✅ Сохранение прогресса на сервере  
✅ Синхронизация между устройствами  
✅ Все 19 задач  
✅ Все 10 уроков  
✅ Система достижений  
✅ Темная тема  
✅ Дашборд  

---

## 🔄 Автоматические обновления

Теперь при каждом `git push`:
- Netlify автоматически обновит фронтенд
- Render автоматически обновит бэкенд

```powershell
# Внесите изменения
git add .
git commit -m "Описание изменений"
git push

# Деплой произойдет автоматически!
```

---

## 🆘 Решение проблем

### Backend не запускается на Render

1. Проверьте логи в Render Dashboard
2. Убедитесь, что `MONGODB_URI` правильный
3. Проверьте, что в MongoDB Atlas разрешен доступ с любого IP

### Frontend не подключается к Backend

1. Проверьте переменную `REACT_APP_API_BASE` в Netlify
2. Убедитесь, что backend работает (откройте URL в браузере)
3. Проверьте `FRONTEND_URL` в Render

### Ошибка CORS

1. Убедитесь, что `FRONTEND_URL` в Render правильный
2. Должен быть без слеша в конце
3. Должен совпадать с URL Netlify

### Backend "засыпает" (Free план Render)

На бесплатном плане Render сервис засыпает после 15 минут неактивности.
- Первый запрос может занять 30-60 секунд
- Это нормально для бесплатного плана

---

## 💡 Советы

1. **Сохраните все URL и пароли** в безопасном месте
2. **Регулярно делайте git push** для обновления
3. **Проверяйте логи** в Render и Netlify при проблемах
4. **Используйте разные пароли** для MongoDB и приложения

---

## 🎓 Дополнительно

### Добавить ИИ-подсказки (опционально)

1. Получите API ключ OpenAI: https://platform.openai.com/api-keys
2. В Render добавьте переменную:
   - Key: `OPENAI_API_KEY`
   - Value: `sk-...ваш ключ`
3. Сохраните и перезапустите

### Настроить custom domain (опционально)

1. Купите домен (например, на Namecheap)
2. В Netlify: Domain settings → Add custom domain
3. Следуйте инструкциям Netlify

---

## 🎉 Поздравляем!

Ваше приложение **CodeTutor** теперь доступно онлайн с полным функционалом!

**Поделитесь ссылкой:**
https://code-tutor-blacknoire14.netlify.app

---

**Удачи с проектом!** 🚀
